<?xml version="1.0" encoding="UTF-8"?>
<persistence version="3.0"
             xmlns="https://jakarta.ee/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/persistence
             http://xmlns.jcp.org/xml/ns/persistence/persistence_2_2.xsd">

    <!--
        This project uses JDBC directly (not JPA/Hibernate) to demonstrate
        explicit SQL and connection pooling control, which better illustrates
        the DataSource and JNDI concepts required by the assignment.

        If migrating to JPA, configure persistence-unit here:
    -->
    <persistence-unit name="TechMartPU" transaction-type="JTA">
        <jta-data-source>jdbc/TechMartDS</jta-data-source>
        <properties>
            <!-- Validate schema on startup rather than auto-creating -->
            <property name="jakarta.persistence.schema-generation.database.action" value="validate"/>
            <!-- Show SQL for debugging (disable in production) -->
            <property name="hibernate.show_sql" value="false"/>
            <property name="hibernate.format_sql" value="true"/>
            <!-- Second-level cache via EclipseLink cache (GlassFish default JPA provider) -->
            <property name="hibernate.cache.use_second_level_cache" value="true"/>
            <property name="hibernate.cache.region.factory_class"
                      value="org.eclipse.persistence.jpa.PersistenceProvider"/>
        </properties>
    </persistence-unit>

</persistence>
